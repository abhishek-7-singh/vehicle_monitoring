import 'package:flutter/material.dart';

class PredictiveMaintenanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Predictive Maintenance')),
      body: Center(child: Text('AI-based maintenance predictions will be shown here')),
    );
  }
}
